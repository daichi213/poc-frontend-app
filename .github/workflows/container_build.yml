name: container_build

on:
  workflow_dispatch:

jobs:
  container_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: docker
        shell: bash
        run: |
          docker -v
